import { useState } from 'react';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { ScrollArea } from '@/components/ui/scroll-area';
import { Separator } from '@/components/ui/separator';
import { 
  FileText, Download, BookOpen, ShoppingCart, Package, 
  MessageCircle, Settings, Users, Truck, Building2, 
  Database, Shield, Layers, BarChart3, Globe, FileDown
} from 'lucide-react';
import { useToast } from '@/hooks/use-toast';
import { useCompanyLogo } from '@/hooks/useCompanyLogo';
import jsPDF from 'jspdf';

const documentationContent = `
# СИСТЕМА ЗА УПРАВЛЕНИЕ НА ПОРЪЧКИ И СКЛАДОВО СТОПАНСТВО
## Пълна документация v1.2

---

## 1. ОБЩ ПРЕГЛЕД

Системата е цялостно решение за управление на онлайн бизнес, включващо:
- Управление на поръчки от различни платформи
- Складово стопанство и инвентар
- Резервация и изписване на наличности
- Интеграция с куриерски услуги
- Автоматични съобщения до клиенти чрез Connectix (Viber/SMS)
- Проверка на клиенти чрез Nekorekten API
- Издаване на фактури
- Синхронизация с e-commerce платформи
- Лизингови статуси за TBI, BNP и UniCredit
- Редактор на интерфейса за персонализация

---

## 2. МОДУЛ "ПОРЪЧКИ" (/)

### 2.1 Преглед на поръчки
- **Таблица с поръчки**: Показва всички поръчки с възможност за сортиране по всяка колона
- **Филтриране**: По статус, източник, дата, куриер, телефон, продукт, каталожен номер
- **Търсене**: Бързо търсене по клиент, телефон, ID и каталожен номер
- **Селекция**: Избор на множество поръчки за групови действия
- **Пагинация**: Страници по 100 поръчки

### 2.2 Полета в поръчка
| Поле | Описание |
|------|----------|
| ID | Уникален номер на поръчката |
| Източник | Откъде е дошла поръчката (WooCommerce, Google, Facebook и др.) |
| Дата | Дата и час на създаване |
| Клиент | Име на клиента |
| Коректност | Флаг дали клиентът е коректен (от Nekorekten) |
| Телефон | Телефонен номер с флаг на държава |
| Цена | Обща стойност в EUR |
| Продукт | Име на продукта (поддържа множество продукти) |
| Каталожен номер | SKU/Код на продукта |
| Количество | Брой единици |
| Доставка | Адрес за доставка |
| Куриер | Куриерска фирма и товарителница |
| Статус | Текущ статус на поръчката |
| Коментар | Вътрешни бележки |

### 2.3 Множество продукти в поръчка
Системата поддържа добавяне на множество продукти към една поръчка:
- В "Нова поръчка" и "Редактиране" има бутон "+ Добави продукт" (син цвят)
- Всеки продукт има отделни полета за име, каталожен номер, количество и цена
- Цената се показва в зелен цвят
- Продуктите се показват като списък в таблицата
- При повече от 1 бр. се показва червен бадж с количеството

### 2.4 Действия с поръчки
- **Редактиране**: Промяна на всички полета включително множество продукти
- **Фактура**: Издаване на фактура за поръчката
- **Печат**: Отпечатване на поръчката
- **Товарителница**: Отваряне на линк за проследяване
- **Viber/SMS**: Изпращане на съобщение до клиента чрез Connectix
- **Проверка в Nekorekten**: Ръчна проверка дали клиентът е коректен
- **Изтриване**: Изтриване на поръчка

### 2.5 Групови действия
- Промяна на статус на множество поръчки
- Изтриване на множество поръчки
- Печат на множество фактури

### 2.6 Статуси на поръчки
Статусите са конфигурируеми и могат да се пренареждат чрез drag & drop.
Примерни статуси: Нова, В обработка, Потвърдена, Изпратена, Доставена, Отказана.

#### Лизингови статуси
Специални статуси за поръчки на лизинг:
- **На лизинг през TBI** - показва се с червено тикче
- **На лизинг през BNP** - показва се с червено тикче
- **На лизинг през UniCredit** - показва се с червено тикче

### 2.7 Управление на складови наличности

#### Резервация на наличност
При смяна на статуса на "В обработка" (или друг конфигуриран статус):
1. Системата търси продукт в склада
2. Резервира необходимото количество (увеличава reserved_stock)
3. Резервираната наличност се вижда в таблицата с продукти

#### Освобождаване на резервация
При смяна на статус ОТ "В обработка" КЪМ друг статус (различен от Изпратена/Отказана):
- Системата автоматично освобождава резервираната наличност

#### Изписване от склада
При смяна на статуса на "Изпратена" (или друг конфигуриран статус):
1. Търси продукт в склада по каталожен номер или име
2. Създава движение "изход" от склада
3. Намалява наличността на продукта
4. Освобождава резервацията

#### Възстановяване на наличност
При смяна на статуса на "Отказана" (или друг конфигуриран статус):
- Възстановява изписаното количество обратно в склада

---

## 3. NEKOREKTEN ИНТЕГРАЦИЯ

### 3.1 Какво е Nekorekten?
Nekorekten е услуга за проверка на коректността на клиенти в България.
Системата се интегрира с API-то на nekorekten.com за автоматична проверка.

### 3.2 Иконки за статус на клиент
- 🟢 **Зелено тикче**: Клиентът е коректен
- 🔴 **Червен X**: Клиентът е некоректен
- ⚫ **Сиви точки**: Проверката се извършва или чака
- ❓ **Въпросителен знак**: Няма данни за клиента

### 3.3 Настройка
1. Отидете в Настройки → Некоректен
2. Въведете API ключ (от nekorekten.com)
3. Включете интеграцията
4. Натиснете "Запази"
5. Използвайте "Тест на връзката" за проверка

### 3.4 Статистика
В страница "Некоректен" се показва статистика за проверените клиенти:
- Брой коректни
- Брой некоректни
- Брой неизвестни
- Общо проверки

---

## 4. CONNECTIX ИНТЕГРАЦИЯ (Viber/SMS)

### 4.1 Какво е Connectix?
Connectix е услуга за изпращане на Viber и SMS съобщения до клиенти.

### 4.2 Канали
- **Viber**: Съобщения през Viber Business (по-евтино)
- **SMS**: Текстови съобщения (резервен вариант)

### 4.3 Статуси на съобщения
- **Pending**: Изчакващо
- **Sent**: Изпратено
- **Delivered**: Доставено
- **Read**: Прочетено
- **Failed**: Неуспешно

### 4.4 Настройка
1. Отидете в Настройки → Connectix
2. Въведете API ключ
3. Изберете режим (Production/Sandbox)
4. Изберете статус за автоматично изпращане
5. Натиснете "Запази"

### 4.5 Автоматични тригери
Може да настроите автоматично изпращане на съобщение при промяна на статус.
Например при "Изпратена" да се изпрати съобщение с номер на товарителница.

---

## 5. МОДУЛ "ИНВЕНТАР" (/inventory)

### 5.1 Продукти
- **Списък**: Всички продукти с наличности, цени, категории
- **Добавяне**: Нов продукт с всички детайли
- **Редактиране**: Промяна на продукт
- **Баркод скенер**: Бързо търсене по баркод

### 5.2 Комплекти (Bundles)
Продуктите могат да бъдат комплекти от други продукти.
**ВАЖНО**: Комплектът няма собствена наличност - само компонентите му имат!

При изписване на комплект:
- Автоматично се изписват компонентите
- Ако някой компонент има недостатъчна наличност, се показва предупреждение
- Наличността на комплекта зависи от минималната наличност на компонентите

### 5.3 Категории
- Йерархична структура на категории
- Подкатегории
- Описание на категория

### 5.4 Доставчици
- Списък на доставчици
- Данни: Име, ЕИК, ДДС номер, адрес, телефон, имейл
- Контактно лице
- Бележки

### 5.5 Движения
Всяко движение на стока се записва:
- **Вход (in)**: Получаване на стока
- **Изход (out)**: Изписване на стока
- **Корекция (adjustment)**: Корекция при инвентаризация
- **Връщане (return)**: Върната стока

### 5.6 WooCommerce синхронизация
- Синхронизация на продукти от WooCommerce
- Актуализация на наличности към WooCommerce
- Ръчна или автоматична синхронизация

---

## 6. МОДУЛ "СЪОБЩЕНИЯ" (/messages)

Преглед на всички изпратени съобщения до клиенти чрез Connectix API.
- История на съобщенията
- Статус на доставка
- Филтриране по канал (Viber/SMS)

---

## 7. МОДУЛ "НАСТРОЙКИ" (/settings)

### 7.1 API
Основни API настройки:
- WooCommerce URL, Consumer Key, Consumer Secret
- Webhook URL за получаване на поръчки

### 7.2 Платформи
Интегрирани e-commerce платформи:
- **WooCommerce**: Пълна интеграция
- **OpenCart**: Webhook интеграция
- **Shopify**: Webhook интеграция
- **PrestaShop**: Webhook интеграция
- **Magento**: Webhook интеграция

### 7.3 Източници
Конфигуриране на източници на поръчки с лого и линк.

### 7.4 Куриери
Списък с куриерски фирми:
- Име на куриер
- Лого
- URL pattern за проследяване

При въвеждане на tracking URL, куриерът се разпознава автоматично и се заключва.

### 7.5 Статуси
Конфигуриране на статуси на поръчки:
- Име, цвят, икона
- Подредба чрез drag & drop
- Изтриване с кофа

#### Настройки за склад
- **Статус за резервиране**: При този статус се резервира наличност (по подразбиране: "В обработка")
- **Статус за изписване**: При този статус се изписва от склада (по подразбиране: "Изпратена")
- **Статус за възстановяване**: При този статус се възстановява наличността (по подразбиране: "Отказана")

### 7.6 Фирма
Фирмени данни за фактури и брандинг.

### 7.7 Потребители (само за администратори)
Управление на потребители и роли.

### 7.8 Некоректен
Настройки за Nekorekten API интеграция с бутони Запази и Тест.

### 7.9 Connectix
Настройки за Viber/SMS съобщения с бутони Запази и Тест.

### 7.10 Редактор на интерфейса
Редактиране на текстове, бутони и иконки в системата без код:
- Промяна на заглавия и етикети
- Смяна на иконки
- Персонализация на бутони
- Превод на всички текстове

### 7.11 Документация
Преглед и изтегляне на пълна документация (MD и PDF формат).

---

## 8. WEBHOOKS

Системата поддържа webhooks за автоматично получаване на поръчки:

| Платформа | Endpoint |
|-----------|----------|
| WooCommerce | /functions/v1/woocommerce-webhook |
| OpenCart | /functions/v1/opencart-webhook |
| Shopify | /functions/v1/shopify-webhook |
| PrestaShop | /functions/v1/prestashop-webhook |
| Magento | /functions/v1/magento-webhook |

---

## 9. БАЗА ДАННИ

### Основни таблици:
- **orders**: Поръчки (с is_correct поле за Nekorekten)
- **order_statuses**: Статуси на поръчки
- **inventory_products**: Продукти в склада
- **inventory_categories**: Категории
- **stock_movements**: Движения на стоки
- **stock_documents**: Складови документи
- **product_bundles**: Комплекти
- **suppliers**: Доставчици
- **couriers**: Куриери
- **ecommerce_platforms**: Платформи
- **invoices**: Фактури
- **connectix_messages**: Съобщения
- **api_settings**: API настройки
- **company_settings**: Фирмени данни
- **allowed_users**: Разрешени потребители

---

## 10. СИГУРНОСТ

- Row Level Security (RLS) на всички таблици
- Само автентикирани потребители имат достъп
- Разрешен списък с потребители
- API ключовете се пазят криптирани
- Service Role ключ само за edge functions

---

## 11. ТЕХНОЛОГИИ

- **Frontend**: React, TypeScript, Vite
- **UI**: Tailwind CSS, shadcn/ui, Framer Motion
- **Backend**: Supabase (PostgreSQL, Edge Functions)
- **Hosting**: Lovable Cloud
- **Auth**: Supabase Auth
- **Storage**: Supabase Storage
- **Интеграции**: Nekorekten, Connectix, WooCommerce, Shopify и др.

---

## 12. КОНТАКТИ И ПОДДРЪЖКА

За въпроси и поддръжка се свържете с администратора на системата.

---

Документация генерирана на: ${new Date().toLocaleDateString('bg-BG')}
`;

export const DocumentationTab = () => {
  const { toast } = useToast();
  const { logoUrl } = useCompanyLogo();
  const [downloading, setDownloading] = useState(false);
  const [downloadingPdf, setDownloadingPdf] = useState(false);

  const handleDownload = () => {
    setDownloading(true);
    try {
      const blob = new Blob([documentationContent], { type: 'text/markdown;charset=utf-8' });
      const url = URL.createObjectURL(blob);
      const link = document.createElement('a');
      link.href = url;
      link.download = `документация-система-${new Date().toISOString().split('T')[0]}.md`;
      document.body.appendChild(link);
      link.click();
      document.body.removeChild(link);
      URL.revokeObjectURL(url);
      
      toast({
        title: 'Успех',
        description: 'Документацията беше изтеглена успешно',
      });
    } catch (error) {
      toast({
        title: 'Грешка',
        description: 'Неуспешно изтегляне на документацията',
        variant: 'destructive',
      });
    } finally {
      setDownloading(false);
    }
  };

  const handleDownloadPdf = async () => {
    setDownloadingPdf(true);
    try {
      const doc = new jsPDF({
        orientation: 'portrait',
        unit: 'mm',
        format: 'a4'
      });

      // Add logo if available
      let startY = 25;
      if (logoUrl) {
        try {
          const img = new Image();
          img.crossOrigin = 'anonymous';
          await new Promise((resolve, reject) => {
            img.onload = resolve;
            img.onerror = reject;
            img.src = logoUrl;
          });
          doc.addImage(img, 'PNG', 15, 10, 40, 20);
          startY = 45;
        } catch (e) {
          console.log('Логото не може да бъде добавено към PDF');
        }
      }

      // Title - Note about transliteration
      doc.setFontSize(18);
      doc.setFont('helvetica', 'bold');
      doc.text('SISTEMA ZA UPRAVLENIE NA', 105, startY, { align: 'center' });
      doc.text('PORACHKI I SKLADOVO STOPANSTVO', 105, startY + 8, { align: 'center' });
      
      doc.setFontSize(12);
      doc.setFont('helvetica', 'normal');
      doc.text('Palna dokumentaciya v1.0', 105, startY + 18, { align: 'center' });

      // Note about transliteration
      doc.setFontSize(9);
      doc.setTextColor(100, 100, 100);
      doc.text('* Tekstut e transliteriran ot Kirilica za PDF format', 105, startY + 26, { align: 'center' });
      doc.text('Za palen tekst na Bulgarski, svalette MD faila', 105, startY + 31, { align: 'center' });

      // Date
      doc.setFontSize(10);
      doc.setTextColor(0, 0, 0);
      const dateStr = new Date().toLocaleDateString('bg-BG');
      doc.text('Generirana na: ' + dateStr, 105, startY + 38, { align: 'center' });

      // Separator line
      doc.setDrawColor(200, 200, 200);
      doc.line(15, startY + 44, 195, startY + 44);

      let yPosition = startY + 54;
      const pageHeight = 280;
      const lineHeight = 6;
      const marginLeft = 15;
      const maxWidth = 180;

      // Bulgarian to Latin transliteration map
      const transliterate = (text: string): string => {
        const map: { [key: string]: string } = {
          'а': 'a', 'б': 'b', 'в': 'v', 'г': 'g', 'д': 'd', 'е': 'e', 'ж': 'zh',
          'з': 'z', 'и': 'i', 'й': 'y', 'к': 'k', 'л': 'l', 'м': 'm', 'н': 'n',
          'о': 'o', 'п': 'p', 'р': 'r', 'с': 's', 'т': 't', 'у': 'u', 'ф': 'f',
          'х': 'h', 'ц': 'ts', 'ч': 'ch', 'ш': 'sh', 'щ': 'sht', 'ъ': 'a', 'ь': '',
          'ю': 'yu', 'я': 'ya',
          'А': 'A', 'Б': 'B', 'В': 'V', 'Г': 'G', 'Д': 'D', 'Е': 'E', 'Ж': 'Zh',
          'З': 'Z', 'И': 'I', 'Й': 'Y', 'К': 'K', 'Л': 'L', 'М': 'M', 'Н': 'N',
          'О': 'O', 'П': 'P', 'Р': 'R', 'С': 'S', 'Т': 'T', 'У': 'U', 'Ф': 'F',
          'Х': 'H', 'Ц': 'Ts', 'Ч': 'Ch', 'Ш': 'Sh', 'Щ': 'Sht', 'Ъ': 'A', 'Ь': '',
          'Ю': 'Yu', 'Я': 'Ya'
        };
        return text.split('').map(char => map[char] || char).join('');
      };

      // Process content
      const lines = documentationContent.split('\n');
      
      for (const line of lines) {
        if (yPosition > pageHeight) {
          doc.addPage();
          yPosition = 20;
        }

        const trimmedLine = line.trim();
        const latinLine = transliterate(trimmedLine);
        
        if (trimmedLine.startsWith('# ')) {
          doc.setFontSize(16);
          doc.setFont('helvetica', 'bold');
          doc.setTextColor(180, 50, 50);
          const text = latinLine.replace('# ', '');
          doc.text(text, marginLeft, yPosition);
          yPosition += lineHeight * 1.5;
        } else if (trimmedLine.startsWith('## ')) {
          doc.setFontSize(14);
          doc.setFont('helvetica', 'bold');
          doc.setTextColor(60, 60, 60);
          const text = latinLine.replace('## ', '');
          doc.text(text, marginLeft, yPosition);
          yPosition += lineHeight * 1.3;
        } else if (trimmedLine.startsWith('### ')) {
          doc.setFontSize(12);
          doc.setFont('helvetica', 'bold');
          doc.setTextColor(80, 80, 80);
          const text = latinLine.replace('### ', '');
          doc.text(text, marginLeft, yPosition);
          yPosition += lineHeight * 1.2;
        } else if (trimmedLine.startsWith('#### ')) {
          doc.setFontSize(11);
          doc.setFont('helvetica', 'bold');
          doc.setTextColor(100, 100, 100);
          const text = latinLine.replace('#### ', '');
          doc.text(text, marginLeft, yPosition);
          yPosition += lineHeight;
        } else if (trimmedLine.startsWith('- **')) {
          doc.setFontSize(10);
          doc.setFont('helvetica', 'normal');
          doc.setTextColor(40, 40, 40);
          const text = '* ' + latinLine.replace('- **', '').replace('**:', ':').replace('**', '');
          const splitText = doc.splitTextToSize(text, maxWidth);
          doc.text(splitText, marginLeft + 5, yPosition);
          yPosition += lineHeight * splitText.length;
        } else if (trimmedLine.startsWith('- ')) {
          doc.setFontSize(10);
          doc.setFont('helvetica', 'normal');
          doc.setTextColor(40, 40, 40);
          const text = '* ' + latinLine.replace('- ', '');
          const splitText = doc.splitTextToSize(text, maxWidth);
          doc.text(splitText, marginLeft + 5, yPosition);
          yPosition += lineHeight * splitText.length;
        } else if (trimmedLine.startsWith('|')) {
          // Table row
          doc.setFontSize(9);
          doc.setFont('helvetica', 'normal');
          doc.setTextColor(60, 60, 60);
          const text = latinLine.replace(/\|/g, ' | ').trim();
          const splitText = doc.splitTextToSize(text, maxWidth);
          doc.text(splitText, marginLeft, yPosition);
          yPosition += lineHeight * splitText.length;
        } else if (trimmedLine === '---') {
          doc.setDrawColor(200, 200, 200);
          doc.line(marginLeft, yPosition, 195, yPosition);
          yPosition += lineHeight;
        } else if (trimmedLine.length > 0) {
          doc.setFontSize(10);
          doc.setFont('helvetica', 'normal');
          doc.setTextColor(40, 40, 40);
          const cleanText = latinLine.replace(/\*\*/g, '').replace(/\*/g, '');
          const splitText = doc.splitTextToSize(cleanText, maxWidth);
          doc.text(splitText, marginLeft, yPosition);
          yPosition += lineHeight * splitText.length;
        } else {
          yPosition += lineHeight * 0.5;
        }
      }

      // Footer on last page
      const pageCount = doc.getNumberOfPages();
      for (let i = 1; i <= pageCount; i++) {
        doc.setPage(i);
        doc.setFontSize(8);
        doc.setTextColor(150, 150, 150);
        doc.text('Stranitsa ' + i + ' ot ' + pageCount, 105, 290, { align: 'center' });
      }

      doc.save('dokumentaciya-sistema-' + new Date().toISOString().split('T')[0] + '.pdf');
      
      toast({
        title: 'Успех',
        description: 'PDF документацията беше изтеглена успешно',
      });
    } catch (error) {
      console.error('PDF generation error:', error);
      toast({
        title: 'Грешка',
        description: 'Неуспешно генериране на PDF',
        variant: 'destructive',
      });
    } finally {
      setDownloadingPdf(false);
    }
  };

  const sections = [
    { icon: Globe, title: '1. Общ преглед', description: 'Обща информация за системата и нейните възможности' },
    { icon: ShoppingCart, title: '2. Модул "Поръчки"', description: 'Управление на поръчки, статуси, филтри и групови действия' },
    { icon: Package, title: '3. Модул "Инвентар"', description: 'Продукти, категории, доставчици, движения, документи и отчети' },
    { icon: MessageCircle, title: '4. Модул "Съобщения"', description: 'Viber и SMS съобщения до клиенти' },
    { icon: Settings, title: '5. Модул "Настройки"', description: 'API, платформи, източници, брандинг, куриери, статуси, фирмени данни' },
    { icon: Shield, title: '6. Автентикация', description: 'Вход, роли и права на достъп' },
    { icon: Layers, title: '7. Webhooks', description: 'Автоматично получаване на поръчки от платформи' },
    { icon: Database, title: '8. База данни', description: 'Структура на таблиците и връзки' },
    { icon: Shield, title: '9. Сигурност', description: 'RLS политики и защита на данните' },
    { icon: BarChart3, title: '10. Технологии', description: 'Използвани технологии и инструменти' },
  ];

  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
            <div>
              <CardTitle className="flex items-center gap-2">
                <BookOpen className="w-5 h-5" />
                Документация на системата
              </CardTitle>
              <CardDescription className="mt-2">
                Пълна документация за функционалността и използването на системата
              </CardDescription>
            </div>
            <div className="flex gap-2">
              <Button variant="outline" onClick={handleDownload} disabled={downloading}>
                <Download className="w-4 h-4 mr-2" />
                {downloading ? 'Изтегляне...' : 'Свали MD'}
              </Button>
              <Button onClick={handleDownloadPdf} disabled={downloadingPdf} className="bg-rose-600 hover:bg-rose-700">
                <FileDown className="w-4 h-4 mr-2" />
                {downloadingPdf ? 'Генериране...' : 'Свали PDF'}
              </Button>
            </div>
          </div>
        </CardHeader>
        <CardContent>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
            {sections.map((section, index) => (
              <div 
                key={index}
                className="flex items-start gap-3 p-4 rounded-lg border bg-card hover:bg-muted/50 transition-colors"
              >
                <section.icon className="w-5 h-5 text-primary mt-0.5 flex-shrink-0" />
                <div>
                  <h3 className="font-medium text-sm">{section.title}</h3>
                  <p className="text-xs text-muted-foreground mt-1">{section.description}</p>
                </div>
              </div>
            ))}
          </div>

          <Separator className="my-6" />

          <div className="space-y-4">
            <h3 className="font-semibold flex items-center gap-2">
              <FileText className="w-4 h-4" />
              Пълен текст на документацията
            </h3>
            <ScrollArea className="h-[500px] w-full rounded-lg border bg-muted/30 p-4">
              <pre className="text-sm whitespace-pre-wrap font-mono leading-relaxed">
                {documentationContent}
              </pre>
            </ScrollArea>
          </div>
        </CardContent>
      </Card>
    </div>
  );
};
